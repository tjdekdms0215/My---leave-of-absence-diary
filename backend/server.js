const express = require('express');
const cors = require('cors');
const app = express();
const port = 3000; // 서버 포트 번호

app.use(cors()); // 모든 요청 허용 (프론트엔드 연결 위해 필수!)

// 1. 휴학 생활 데이터 (나중에는 DB에서 가져올 거예요)
const timelineData = [
    { id: 1, date: "2023-03", title: "운전면허 취득", desc: "2종 보통 합격! 베스트 드라이버 🚗" },
    { id: 2, date: "2023-05", title: "제주도 여행", desc: "혼자 떠난 첫 여행, 성산일출봉 등반 ✈️" },
    { id: 3, date: "2023-08", title: "카페 알바", desc: "하루에 아메리카노 100잔 내리기 ☕" },
    { id: 4, date: "2024-01", title: "웹사이트 개발", desc: "나만의 포트폴리오 만들기 시작 💻" }
];

// 2. 기본 주소로 접속했을 때
app.get('/', (req, res) => {
    res.send('안녕하세요! 백엔드 서버가 켜져있습니다.');
});

// 3. 데이터 달라고 요청했을 때 (API)
app.get('/api/timeline', (req, res) => {
    res.json(timelineData); // 데이터를 JSON 형식으로 보내줍니다.
});

// 4. 서버 실행
app.listen(port, () => {
    console.log(`서버가 켜졌습니다! http://localhost:${port}`);
});